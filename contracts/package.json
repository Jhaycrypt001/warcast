{
  "name": "warcast-contracts",
  "version": "1.0.0",
  "description": "WARCAST smart contracts on X Layer",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network xlayerTestnet",
    "deploy:agentrank": "hardhat run scripts/deploy_agent_rank.js --network xlayerTestnet"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@types/node": "^22.0.0",
    "dotenv": "^16.6.1",
    "ethers": "^5.8.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
